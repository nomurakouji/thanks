<%= stylesheet_link_tag 'users', media: 'all', 'data-turbolinks-track': 'reload' %>

<%= link_to "ユーザー作成",  new_admin_user_path, class: "btn btn-secondary"%>
<div class = "box">
  <div class = "box-inner">

    <h1>ユーザー一覧</h1>
    <div class = "search-box">
      <%= search_form_for @q, url: admin_users_path do |f| %>
        <%= f.label :name_or_email, "ユーザー名orメールアドレス" %>
        <%= f.search_field :name_or_email_cont %>
        <%= f.submit '検索'%>
      <% end %>
    </div>

     <table class="table table-hover">
      <tr>
        <th><%= sort_link(@q, :id, "ユーザーID") %></th>
        <th><%= sort_link(@d, :department_id, "部門名") %></th>
        <th><%= sort_link(@q, :name, "ユーザー名") %></th>
        <th><%= sort_link(@q, :email, "email") %></th>
        <th></th>
        <th></th>
      </tr>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.department.name %></td> 
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td class = check_buttons><%= link_to "編集",   edit_admin_user_path(user), class: 'btn btn-warning'%></td>
            <td class = check_buttons><%= button_to "削除", admin_users_path(user), method: :delete, data: { confirm: '本当に削除していいですか？' }, class: 'btn btn-danger' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
    <%= link_to "管理画面TOPに戻る", dashboard_path %>
  </div>
</div>