<br>
<%= link_to "ユーザー作成",  new_admin_user_path %>

<h1>ユーザー検索</h1>
<%= search_form_for @q, url: admin_users_path do |f| %>
  <%= f.label :name_or_email, "ユーザー名orメールアドレス" %>
  <%= f.search_field :name_or_email_cont %>
  <%= f.submit '検索'%>
<% end %>

<table>
  <tr>
    <th><%= sort_link(@q, :id, "ユーザーID") %></th>
    <th><%= sort_link(@d, :department_id, "部門名") %></th>
    <th><%= sort_link(@q, :name, "ユーザー名") %></th>
    <th><%= sort_link(@q, :email, "email") %></th>
  </tr>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.department.name %></td> 
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to "Edit",  edit_admin_user_path(user) %></td>
        <td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "管理画面に戻る", dashboard_path %>